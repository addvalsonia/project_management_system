<% if @organizations.size > 0 %>
	<%= select(nil, :organization_id,@organizations.collect{|c|[c.org_name,c.id]},
	{:include_blank => true}, :style => "width:150px")%>
<% else %>
	<%= select(nil, :organization_id,["No Organization",0],{:include_blank => true}, :style => "width:150px")%>
<% end %>

